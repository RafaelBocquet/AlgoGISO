Convention :
  If an argument is passed as a non pointer, its ownership is given
  Otherwise, its owner doesn't change

TODO : const where needed
TODO : pick best between O(n + m) and O (n log n) sorts